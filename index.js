document.addEventListener('DOMContentLoaded', function () {
    button.addEventListener('click', function () mOver(obj) {
      obj.innerHTML = "$300 Dead Stock"
    }
function mOut(obj) {
      obj.innerHTML = "Buy Now"
    }
    function mOver(obj) {
      obj.innerHTML = "$600 Dead Stock"
    }

    function mOut(obj) {
      obj.innerHTML = "Buy Now"
    }
    function mOver(obj) {
      obj.innerHTML = "$190 Dead Stock"
    }

    function mOut(obj) {
      obj.innerHTML = "Buy Now"
    }
    console.log(testfile)
    });
  });